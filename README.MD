# DCP Unpacker

```
usage: dcp_unpacker.py [-h] [-e E] filename

Unpack DCP files of Wintermute Engine games

positional arguments:
  filename    Path to a DCP file

options:
  -h, --help  show this help message and exit
  -e E        Use custom encoding for intertal paths (default one is ascii)
```
